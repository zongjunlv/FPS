# AGENTS.md

## Agent skills

### Issue tracker

项目任务使用 `zongjunlv/FPS` 的 GitHub Issues 管理；外部 Pull Request 不作为分诊入口。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用 `needs-triage`、`needs-info`、`ready-for-agent`、`ready-for-human`、`wontfix` 五个标准分诊标签。详见 `docs/agents/triage-labels.md`。

### Domain docs

项目采用单上下文结构，领域说明位于根目录 `CONTEXT.md`，架构决策位于 `docs/adr/`。详见 `docs/agents/domain.md`。
